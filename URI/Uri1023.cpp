#include<bits/stdc++.h>
using namespace std;

struct v{
	z=int;
	x=int;
};

int main ()
{
	for(int i=0;;i++)
	{
		cin >> a;
		if(a==0)
			break;
		else
		{
			for(int j=0;j<a;j++)
			{
				cin >> st.z[j >> st.x;
				d+=b[j];
				e+=c[j];
				g=e/d
			}
			cout << Cidade# << "i+1" << endl;
				for(int j=0;j<a;j++)
				{
					if (j==a-1)
						cout << b[j] << "-" << (c[j])/b[j]) << endl;
					else
						cout << b[j] << "-" << (c[j])/b[j]) << " ";
				cout << "Consumo medio: " << fixed << setprecision(2) << g << " m3" << endl;
				}
		}
	}
	return 0;
}